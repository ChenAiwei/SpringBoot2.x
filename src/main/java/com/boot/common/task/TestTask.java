package com.boot.common.task;

import org.springframework.stereotype.Component;

@Component
public class TestTask {

   /* @Scheduled(cron = "0/2 * * * * ? ")
    public  void doTask() throws InterruptedException {
        Thread.sleep(10000);
        System.out.println("任务一："+System.currentTimeMillis());
    }

    @Scheduled(cron = "0/2 * * * * ? ")
    public  void doTask2(){
        System.out.println("任务二："+System.currentTimeMillis());
    }
*/
}
